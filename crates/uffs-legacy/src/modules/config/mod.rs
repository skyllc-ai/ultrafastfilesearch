// ├── config/                     # Configuration management (global settings,
// paths, ignored files) │   ├── mod.rs                  # Configuration
// management entry point │   └── settings.rs             # Parsing and managing
// config options (e.g., via YAML, TOML, JSON)

mod settings;
